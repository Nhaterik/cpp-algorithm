#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        string n;
        long long m,s=0;
        cin >> n >> m;
        for(int i=0;i<n.size();i++)
        s=(s*10+n[i]-'0')%m;
        cout<<s<<endl;
    }
    return 0;
}
